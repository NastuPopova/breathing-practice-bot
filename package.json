{
  "name": "breathing-practice-bot",
  "version": "1.0.0",
  "description": "Telegram bot for breathing practices",
  "main": "index.js",
  "scripts": {
    "start": "DEBUG=telegraf:* node index.js",
    "dev": "DEBUG=telegraf:* nodemon index.js",
    "ping": "node -e \"require('./ping').setupPing(process.env.APP_URL || 'http://localhost:3000', 5)\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "telegraf": "^4.12.2",
    "express": "^4.18.2",
    "node-fetch": "^2.6.9"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": "16.x"
  }
}
